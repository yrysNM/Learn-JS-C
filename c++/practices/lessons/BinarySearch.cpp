#include <cmath>
#include <iostream>

using namespace std;

int binarySearch(int arr[], int x) {
  int right = sizeof(*arr) / sizeof(arr);
  int left = 0;
  while (left <= right) {
    int mid = right + left / 2;
  }
  return 0;
}

int main() {
  int testArr[] = {2, 7, 3, 11, 20, 25, 27, 50, 51, 60};
  cout << sizeof(testArr) << " " << sizeof(*testArr);
  return 0;
}