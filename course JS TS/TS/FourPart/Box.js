"use strict";
class Box {
    width;
    height;
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
}
const firstBox = new Box(10, 22);
console.log(firstBox);
